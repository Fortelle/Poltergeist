<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Poltergeist.Views.Options.RatingOptionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Poltergeist.Views.Options"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <RatingControl
        x:Name="RatingControl1"
        HorizontalAlignment="Left"
        IsClearEnabled="{x:Bind Item.AllowsEmpty, Mode=OneTime}" 
        MaxRating="{x:Bind Item.MaxRating, Mode=OneTime}"
        Value="{x:Bind Value, Mode=TwoWay}"
        ValueChanged="RatingControl_ValueChanged"
        >
    </RatingControl>
</UserControl>
