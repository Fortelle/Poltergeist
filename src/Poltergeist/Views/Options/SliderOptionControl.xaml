<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Poltergeist.Views.Options.SliderOptionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Poltergeist.Views.Options"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        VerticalAlignment="Center"
        HorizontalAlignment="Stretch"
        >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".3*"/>
            <ColumnDefinition Width=".7*"/>
        </Grid.ColumnDefinitions>
        <Slider
            Grid.Column="1"
            VerticalAlignment="Center"
            Value="{x:Bind Value, Mode=TwoWay}"
            Minimum="{x:Bind Minimum, Mode=OneTime}"
            Maximum="{x:Bind Maximum, Mode=OneTime}"
            StepFrequency="{x:Bind StepFrequency, Mode=OneTime}"
            SmallChange="{x:Bind StepFrequency, Mode=OneTime}"
            IsThumbToolTipEnabled="False"
            >
        </Slider>
        <TextBlock
            Grid.Column="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Text="{x:Bind Text, Mode=OneWay}" 
            Margin="16, 0"
            />
    </Grid>
</UserControl>
