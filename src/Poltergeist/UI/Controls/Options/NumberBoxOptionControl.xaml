<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Poltergeist.UI.Controls.Options.NumberBoxOptionControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Poltergeist.UI.Controls.Options"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <NumberBox
        SpinButtonPlacementMode="Inline"
        HorizontalAlignment="Stretch"
        Value="{x:Bind Value, Mode=TwoWay}"
        Minimum="{x:Bind Minimum, Mode=OneTime}"
        Maximum="{x:Bind Maximum, Mode=OneTime}"
        SmallChange="{x:Bind SmallChange, Mode=OneTime}"
        >
    </NumberBox>
</UserControl>
