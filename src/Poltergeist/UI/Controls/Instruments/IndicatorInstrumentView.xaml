<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Poltergeist.UI.Controls.Instruments.IndicatorInstrumentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Poltergeist.UI.Controls.Instruments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="TileInstrumentUserControl"
    mc:Ignorable="d">
    <ItemsControl
        ItemsSource="{x:Bind ViewModel.Items}"
        FontSize="{x:Bind ViewModel.FontSize}"
        >
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <ItemsWrapGrid
                    Orientation="Horizontal"
                    ItemWidth="{Binding ElementName=TileInstrumentUserControl, Path=ViewModel.IconWidth}"
                    ItemHeight="{Binding ElementName=TileInstrumentUserControl, Path=ViewModel.IconHeight}"
                    />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <local:IndicatorIconView
                    Margin="0, 0, 4, 4"
                    >
                </local:IndicatorIconView>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
