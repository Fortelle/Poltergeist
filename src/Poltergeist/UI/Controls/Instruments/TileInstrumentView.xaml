<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Poltergeist.UI.Controls.Instruments.TileInstrumentView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Poltergeist.UI.Controls.Instruments"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Poltergeist.UI.Controls"
    x:Name="TileInstrumentUserControl"
    mc:Ignorable="d"
    >

    <ItemsRepeater
        ItemsSource="{x:Bind ViewModel.Items, Mode=OneTime}"
        >
        <ItemsRepeater.Layout>
            <UniformGridLayout
                Orientation="Horizontal"
                MinItemHeight="{x:Bind ViewModel.IconHeight, Mode=OneTime}"
                MinItemWidth="{x:Bind ViewModel.IconWidth, Mode=OneTime}"
                MinRowSpacing="4"
                MinColumnSpacing="4"
                />
        </ItemsRepeater.Layout>
        <ItemsRepeater.ItemTemplate>
            <DataTemplate>
                <Border
                    Background="{Binding Background, Mode=OneTime, TargetNullValue=#DDDDDD}"
                    CornerRadius="4"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    ToolTipService.ToolTip="{Binding Tooltip, Mode=OneTime}"
                    >
                    <controls:IconControl
                        VerticalAlignment="Stretch"
                        HorizontalAlignment="Stretch"
                        Foreground="{Binding Foreground, Mode=OneTime}" 
                        FontSize="{Binding ViewModel.FontSize, ElementName=TileInstrumentUserControl, Mode=OneTime}"
                        Icon="{Binding Icon, Mode=OneTime}"
                        >
                    </controls:IconControl>
                </Border>
            </DataTemplate>
        </ItemsRepeater.ItemTemplate>
    </ItemsRepeater>
</UserControl>
